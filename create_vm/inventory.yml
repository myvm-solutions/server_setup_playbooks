vmhosts:
  hosts:
    rock64vm2:
    # rockprovm:
  vars:
    # ansible_become_password: "{{ ansible_password }}"
vmguests:
  hosts:
    # DB-Master:
    DB-Cluster1:
      clone_id: "1"
      clone_from: DB-Master
    DB-Cluster2:
      clone_id: "2"
      clone_from: DB-Master      
    DB-Cluster3:
      clone_id: "3"
      clone_from: DB-Master      